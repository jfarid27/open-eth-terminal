# /tmp

This folder is a temporary folder for outputs. Most Actions will
generally dump files that are worth saving here.

Developers should not check in files in this folder. And it is
blocked in .gitignore.